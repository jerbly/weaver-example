groups:
  - id: registry.example
    type: attribute_group
    display_name: Example Attributes
    brief: "Describes example attributes."
    attributes:
      - id: example.message
        type: string
        brief: A simple message.
        note: "This attribute is used to demonstrate a simple string attribute."
        stability: development
        examples: ["Hello, World!"]
      - ref: host.name
      - ref: host.arch
      - ref: service.name
      - ref: telemetry.sdk.language
      - ref: telemetry.sdk.version
      - ref: telemetry.sdk.name
